import { ResaltarCitaDirective } from '../directives/resaltarcita.directive';

describe('ResaltarcitaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarCitaDirective();
    expect(directive).toBeTruthy();
  });
});
